{{ dump()}}

{% for platform in platforms %}
    {#    <div class="row">
            <div class="panel panel-default">
                <div class="panel-heading"> {{ platform.name }}</div>
                <div class="panel-body">
                    {{ platform.overview }}
                </div>
                <div class="panel-footer">Panel footer</div>
            </div>
        </div>#}

    <div class="platorm_card row">
        <div class="col-md-2">
            <img class="img-responsive" src="{{ asset('images/generique.png') }}" alt="image" />
           {# {% if produit.image == "" %}
                <img class="img-responsive" src="{{ asset('images/generique.png') }}" alt="image" />
            {% else %}
                <img class="img-responsive" src="{{ asset( produit.image) }}" alt="image" />
            {% endif %}#}
        </div>

        <div class="col-md-10">


            <div class="row">
                <div class="col-xs-1">
                    {{ platform.id }}
                </div>
                <div class="col-xs-11">
                    {{ platform.name }}
                </div>
            </div>
            <div class="row">

                {{ platform.overview }}

            </div>
        </div>
    </div>


{% endfor %}