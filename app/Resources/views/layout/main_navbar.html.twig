<nav class="navbar navbar-default" role="navigation" id="topnavbar">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}"><i class="fa fa-home"></i>TheGamesDB</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            {{ knp_menu_render('AppBundle:Builder:mainNavBarMenu', {'currentClass': 'active'}) }}

            <div class="navbar-form navbar-left">
                {{ render(controller('AppBundle:Game:search_form')) }}
            </div>

            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <!-- Logged -->
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="fa fa-user"></i> {{app.user.username}} <span class="caret"></span>
                        </a>
                        {{ knp_menu_render('AppBundle:Builder:userLoggedMenu', {'currentClass': 'active'}) }}
    
                    {% else %}
                        <!-- Not Logged -->
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Mon compte <span class="caret"></span></a>

                        {{ knp_menu_render('AppBundle:Builder:userNotLoggedMenu', {'currentClass': 'active'}) }}

                    {% endif %}
                </li>
            </ul>

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
                