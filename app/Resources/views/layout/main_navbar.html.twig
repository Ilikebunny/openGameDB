<nav class="navbar navbar-default" role="navigation" id="topnavbar">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}"><i class="fa fa-home"></i>TheGamesDB</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            <ul class="nav navbar-nav">
                <li><a href="{{ path('game') }}">Games</a></li>
                <li><a href="{{ path('homepage') }}">Platforms</a></li>
                <li><a href="{{ path('homepage') }}">Stats</a></li>
                <li><a href="{{ path('nelmio_api_doc_index') }}">API</a></li>
                <li><a href="{{ path('homepage') }}">Forums</a></li>
                <li><a href="{{ path('homepage') }}">Calendar</a></li>
                <li><a href="{{ path('homepage') }}"><span class="label label-default vertical-align">Add new game</span></a></li>
            </ul>

{#            <form class="navbar-form navbar-left">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
            </form>#}
            <div class="navbar-form navbar-left">
                {{ render(controller('AppBundle:Game:search_form')) }}
            </div>
            
            <ul class="nav navbar-nav navbar-right">
                {#                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                    <li><a href="{{ path('admin_dashboard') }}">Administration</a></li>
                                    {% endif %}#}
                <li class="dropdown">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <!-- Logged -->
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="fa fa-user"></i> {{app.user.username}} <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_edit') }}">Editer compte</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}">Mot de passe</a></li>
                            <li role="separator" class="divider"></li>
                                {#                            <li><a href="{{ path('myprofil_edit') }}">Editer profil</a></li>#}
                            <li role="separator" class="divider"></li>
                                {#                            <li><a href="{{ path('admin_dashboard') }}">Administration</a></li>#}
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}</a></li>
                        </ul>
                    {% else %}
                        <!-- Not Logged -->
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Mon compte <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a></li>
                            <li><a href="{{ path('fos_user_resetting_request') }}">Mot de passe oublié</a></li>
                        </ul>
                    {% endif %}
                </li>
            </ul>

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
